package alternativa.tanks.models.tank
{
   internal class §!#]§
   {
      private var §do set with§:Dictionary;
      
      public function §!#]§()
      {
         this.§do set with§ = new Dictionary();
         super();
      }
      
      public function §return var extends§(param1:Tanks3DSResource) : §get for try§
      {
         var _loc2_:§get for try§ = this.§do set with§[param1.id];
         if(_loc2_ == null)
         {
            _loc2_ = new §get for try§(param1);
            this.§do set with§[param1.id] = _loc2_;
         }
         return _loc2_;
      }
   }
}

