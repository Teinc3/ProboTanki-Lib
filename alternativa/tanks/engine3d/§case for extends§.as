package alternativa.tanks.engine3d
{
   import flash.display.BitmapData;
   
   public class §case for extends§ extends §continue package dynamic§ implements EffectsMaterialRegistry
   {
      public function §case for extends§()
      {
         super(new §with set try§());
      }
      
      override protected function getTexture(param1:BitmapData, param2:Boolean) : BitmapData
      {
         return param1;
      }
   }
}

