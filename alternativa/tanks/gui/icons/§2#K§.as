package alternativa.tanks.gui.icons
{
   import flash.display.Bitmap;
   import flash.display.BitmapData;
   
   public class §2#K§
   {
      private static const §throw set import§:Class = §^#N§;
      
      private static const §4Y§:BitmapData = new §throw set import§().bitmapData;
      
      private static const §2#y§:Class = §-e§;
      
      private static const §0! §:BitmapData = new §2#y§().bitmapData;
      
      private static const §-#-§:Class = § "a§;
      
      private static const §with set false§:BitmapData = new §-#-§().bitmapData;
      
      public function §2#K§()
      {
         super();
      }
      
      public static function §finally const var§() : Bitmap
      {
         return new Bitmap(§4Y§);
      }
      
      public static function §switch package default§() : Bitmap
      {
         return new Bitmap(§0! §);
      }
      
      public static function §dynamic package var§() : Bitmap
      {
         return new Bitmap(§with set false§);
      }
   }
}

