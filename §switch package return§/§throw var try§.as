package §switch package return§
{
   import platform.client.fp10.core.model.impl.Model;
   import platform.client.fp10.core.type.IGameObject;
   
   public class §throw var try§ implements §else for override§
   {
      private var object:IGameObject;
      
      private var impl:§else for override§;
      
      public function §throw var try§(param1:IGameObject, param2:§else for override§)
      {
         super();
         this.object = param1;
         this.impl = param2;
      }
      
      public function §^J§() : Number
      {
         var §0!J§:Number = NaN;
         try
         {
            Model.object = this.object;
            §0!J§ = this.impl.§^J§();
         }
         finally
         {
            Model.popObject();
         }
         return §0!J§;
      }
   }
}

