package §[!#§
{
   import alternativa.tanks.service.dailyquest.DailyQuestNotifierService;
   import alternativa.tanks.service.dailyquest.§import for case§;
   import flash.display.Bitmap;
   import flash.display.BitmapData;
   import flash.display.Sprite;
   
   public class §try for const§ extends Sprite
   {
      [Inject]
      public static var §while var const§:DailyQuestNotifierService;
      
      private static var §dynamic var include§:Class = §each const try§;
      
      private static var §`#m§:BitmapData = Bitmap(new §dynamic var include§()).bitmapData;
      
      public function §try for const§()
      {
         super();
         var _loc1_:Bitmap = new Bitmap(§`#m§);
         addChild(_loc1_);
         visible = false;
         §while var const§.addEventListener(§import for case§.§<D§,this.§while package package§);
         §while var const§.addEventListener(§import for case§.§8"I§,this.§!!&§);
      }
      
      private function §!!&§(param1:§import for case§) : void
      {
         visible = false;
      }
      
      private function §while package package§(param1:§import for case§) : void
      {
         visible = true;
      }
   }
}

