package §in var super§
{
   import alternativa.object.ClientObject;
   import alternativa.tanks.models.weapon.§extends for final§;
   
   public class §in default§ extends §extends for final§
   {
      public function §in default§(param1:ClientObject)
      {
         super(param1);
      }
      
      public function §^G§() : §implements const use§
      {
         return §implements const use§(super.§get package break§().turret.getParams(§implements const use§));
      }
   }
}

