package §in const true§
{
   import alternativa.object.ClientObject;
   
   public interface §include var dynamic§
   {
      function §%H§(param1:ClientObject) : §const var if§;
      
      function §@!1§(param1:ClientObject) : §implements package in§;
   }
}

