package §0#b§
{
   import flash.display.Bitmap;
   import flash.display.BitmapData;
   
   public class §return package in§
   {
      private static const §implements package return§:Class = §%Y§;
      
      private static const §@d§:BitmapData = Bitmap(new §implements package return§()).bitmapData;
      
      private static const §extends import§:Class = §7#`§;
      
      private static const §finally var else§:BitmapData = Bitmap(new §extends import§()).bitmapData;
      
      private static const §null catch if§:Class = §set package implements§;
      
      private static const §throw for package§:BitmapData = Bitmap(new §null catch if§()).bitmapData;
      
      private static const §!"]§:Class = §=i§;
      
      private static const §15§:BitmapData = Bitmap(new §!"]§()).bitmapData;
      
      private static const §switch const for§:Class = §9#b§;
      
      private static const §>#-§:BitmapData = Bitmap(new §switch const for§()).bitmapData;
      
      private static const §extends set for§:Class = §6!1§;
      
      private static const §;!n§:BitmapData = Bitmap(new §extends set for§()).bitmapData;
      
      private static const §if set super§:Class = §%X§;
      
      private static const §'!%§:BitmapData = Bitmap(new §if set super§()).bitmapData;
      
      private static const §dynamic var true§:Class = §use break§;
      
      private static const §[!W§:BitmapData = Bitmap(new §dynamic var true§()).bitmapData;
      
      private static const §try catch return§:Class = §override break§;
      
      private static const §5#^§:BitmapData = Bitmap(new §try catch return§()).bitmapData;
      
      private static const §while const throw§:Class = §#"J§;
      
      private static const §'"i§:BitmapData = Bitmap(new §while const throw§()).bitmapData;
      
      private static const §?!o§:Class = §"!`§;
      
      private static const §>!u§:BitmapData = Bitmap(new §?!o§()).bitmapData;
      
      private static const §'2§:Class = §'l§;
      
      private static const §]#-§:BitmapData = Bitmap(new §'2§()).bitmapData;
      
      public function §return package in§()
      {
         super();
      }
      
      public static function §override set var§(param1:Boolean, param2:Boolean) : BitmapData
      {
         return param1 ? (param2 ? §throw for package§ : §@d§) : (param2 ? §15§ : §finally var else§);
      }
      
      public static function §="L§(param1:Boolean, param2:Boolean) : BitmapData
      {
         return param1 ? (param2 ? §'!%§ : §>#-§) : (param2 ? §[!W§ : §;!n§);
      }
      
      public static function §dynamic for use§(param1:Boolean, param2:Boolean) : BitmapData
      {
         return param1 ? (param2 ? §>!u§ : §5#^§) : (param2 ? §]#-§ : §'"i§);
      }
   }
}

