package §if const for§
{
   import alternativa.osgi.OSGi;
   import alternativa.types.Long;
   import platform.client.fp10.core.registry.ModelRegistry;
   import §throw const package§.§"!M§;
   
   public class §each package set§
   {
      private static var modelRegistry:ModelRegistry = ModelRegistry(OSGi.getInstance().getService(ModelRegistry));
      
      private var §do package function§:§"!M§;
      
      public var modelId:int;
      
      public function §each package set§()
      {
         super();
         this.modelId = 2;
         this.§do package function§ = §"!M§(modelRegistry.getModel(Long.getLong(0,300020028)));
      }
      
      public function invoke(param1:Object) : void
      {
         var _loc2_:Object = null;
         var _loc3_:* = param1.getPacketId();
         switch(0)
         {
         }
      }
      
      private function serverHalt() : void
      {
         this.§do package function§.serverHalt();
      }
   }
}

