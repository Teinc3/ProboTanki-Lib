package §super var native§
{
   import alternativa.object.ClientObject;
   
   public interface §package var extends§
   {
      function §var catch for§(param1:ClientObject) : §return for while§;
   }
}

