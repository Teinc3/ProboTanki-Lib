package §super package switch§
{
   import §8"Y§.§!#J§;
   import flash.display.Bitmap;
   import projects.tanks.clients.fp10.libraries.TanksLocale;
   
   public class §<!3§ extends §!#J§
   {
      private static const §5!?§:Class = §in const extends§;
      
      public function §<!3§(param1:String)
      {
         this.preview = new Bitmap(new §5!?§().bitmapData);
         super(param1,localeService.getText(TanksLocale.TEXT_PROMO_CODE_SHOP_LABEL));
         §return for throw§();
      }
      
      override protected function align() : void
      {
         super.align();
         if(preview != null)
         {
            preview.y += 4;
            preview.x -= PADDING;
         }
      }
   }
}

