package §in package catch§
{
   import alternativa.math.Vector3;
   import alternativa.tanks.battle.objects.tank.Tank;
   import platform.client.fp10.core.model.impl.Model;
   import platform.client.fp10.core.type.IGameObject;
   
   public class §while var continue§ implements §+!Q§
   {
      private var object:IGameObject;
      
      private var impl:§+!Q§;
      
      public function §while var continue§(param1:IGameObject, param2:§+!Q§)
      {
         super();
         this.object = param1;
         this.impl = param2;
      }
      
      public function §default const extends§(param1:int) : void
      {
         var §for const break§:int = param1;
         var §-#`§:int = §for const break§;
         try
         {
            Model.object = this.object;
            this.impl.§default const extends§(§-#`§);
         }
         finally
         {
            Model.popObject();
         }
      }
      
      public function §while set in§(param1:int) : void
      {
         var §for const break§:int = param1;
         var §-#`§:int = §for const break§;
         try
         {
            Model.object = this.object;
            this.impl.§while set in§(§-#`§);
         }
         finally
         {
            Model.popObject();
         }
      }
      
      public function §with package use§(param1:int, param2:Vector3, param3:Tank, param4:Vector3) : void
      {
         var §for const break§:int = param1;
         var §!!u§:Vector3 = param2;
         var §`#o§:Tank = param3;
         var §5$"§:Vector3 = param4;
         var §-#`§:int = §for const break§;
         var §switch const false§:Vector3 = §!!u§;
         var §0!O§:Tank = §`#o§;
         try
         {
            Model.object = this.object;
            this.impl.§with package use§(§-#`§,§switch const false§,§0!O§,§5$"§);
         }
         finally
         {
            Model.popObject();
         }
      }
      
      public function §=!l§(param1:int, param2:Vector3) : void
      {
         var §for const break§:int = param1;
         var §!!u§:Vector3 = param2;
         var §-#`§:int = §for const break§;
         var §switch const false§:Vector3 = §!!u§;
         try
         {
            Model.object = this.object;
            this.impl.§=!l§(§-#`§,§switch const false§);
         }
         finally
         {
            Model.popObject();
         }
      }
   }
}

