package §9"o§
{
   import flash.display.Bitmap;
   import flash.display.BitmapData;
   
   public class §implements const true§
   {
      private static const §override for implements§:Class = §>"^§;
      
      public static const §!+§:BitmapData = Bitmap(new §override for implements§()).bitmapData;
      
      private static const §extends for implements§:Class = §2!`§;
      
      public static const §do package while§:BitmapData = Bitmap(new §extends for implements§()).bitmapData;
      
      private static const §2"#§:Class = §if package include§;
      
      public static const §]#x§:BitmapData = Bitmap(new §2"#§()).bitmapData;
      
      private static const §<h§:Class = §1#r§;
      
      public static const §%#3§:BitmapData = Bitmap(new §<h§()).bitmapData;
      
      private static const §import package extends§:Class = §@#§;
      
      public static const §+"w§:BitmapData = Bitmap(new §import package extends§()).bitmapData;
      
      private static const §!$"§:Class = §#n§;
      
      public static const §throw set switch§:BitmapData = Bitmap(new §!$"§()).bitmapData;
      
      private static const §1#9§:Class = §8!G§;
      
      public static const §&"u§:BitmapData = Bitmap(new §1#9§()).bitmapData;
      
      private static const §null const use§:Class = §^"d§;
      
      public static const §in const finally§:BitmapData = Bitmap(new §null const use§()).bitmapData;
      
      private static const §switch const for§:Class = §9!X§;
      
      public static const §override package false§:BitmapData = Bitmap(new §switch const for§()).bitmapData;
      
      private static const §-§:Class = §dynamic set include§;
      
      public static const §return var continue§:BitmapData = Bitmap(new §-§()).bitmapData;
      
      private static const §5!2§:Class = §5##§;
      
      public static const §var var class§:BitmapData = Bitmap(new §5!2§()).bitmapData;
      
      private static const §<#s§:Class = §true include§;
      
      public static const §%# §:BitmapData = Bitmap(new §<#s§()).bitmapData;
      
      public function §implements const true§()
      {
         super();
      }
   }
}

