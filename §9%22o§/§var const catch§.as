package §9"o§
{
   import flash.display.Bitmap;
   import flash.display.BitmapData;
   
   public class §var const catch§
   {
      private static const §override for implements§:Class = §set const set§;
      
      public static const §!+§:BitmapData = Bitmap(new §override for implements§()).bitmapData;
      
      private static const §extends for implements§:Class = §7!h§;
      
      public static const §do package while§:BitmapData = Bitmap(new §extends for implements§()).bitmapData;
      
      private static const §!$"§:Class = §'![§;
      
      public static const §throw set switch§:BitmapData = Bitmap(new §!$"§()).bitmapData;
      
      private static const §implements package return§:Class = §static continue§;
      
      public static const §var package use§:BitmapData = Bitmap(new §implements package return§()).bitmapData;
      
      private static const §-§:Class = §'C§;
      
      public static const §return var continue§:BitmapData = Bitmap(new §-§()).bitmapData;
      
      private static const §5!2§:Class = §with override§;
      
      public static const §var var class§:BitmapData = Bitmap(new §5!2§()).bitmapData;
      
      private static const §<#s§:Class = §override var import§;
      
      public static const §%# §:BitmapData = Bitmap(new §<#s§()).bitmapData;
      
      private static const §`!j§:Class = §["N§;
      
      public static const §7c§:BitmapData = Bitmap(new §`!j§()).bitmapData;
      
      private static const §set catch while§:Class = §include var super§;
      
      public static const §5$$§:BitmapData = Bitmap(new §set catch while§()).bitmapData;
      
      public function §var const catch§()
      {
         super();
      }
   }
}

