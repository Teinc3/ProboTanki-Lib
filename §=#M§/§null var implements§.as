package §=#M§
{
   import alternativa.object.ClientObject;
   import alternativa.tanks.battle.§+$%§;
   import §try const package§.§each var import§;
   import §try const package§.§include var continue§;
   
   public class §null var implements§ extends §include var continue§ implements §each var import§, §'#B§
   {
      public function §null var implements§()
      {
         super();
      }
      
      public function initObject(param1:ClientObject, param2:Number, param3:Number, param4:Number) : void
      {
         var _loc5_:§'#o§;
         (_loc5_ = new §'#o§()).§else var const§ = isNaN(param2) ? 0 : §+$%§.§use final§(param2);
         _loc5_.§override try§ = isNaN(param4) ? 1 : §+$%§.§use final§(param4);
         _loc5_.§function set do§ = isNaN(param3) ? 0 : param3;
         _loc5_.§final var native§ = _loc5_.§override try§ - _loc5_.§else var const§;
         if(_loc5_.§function set do§ > 100)
         {
            _loc5_.§function set do§ = 100;
         }
         param1.putParams(§null var implements§,_loc5_);
      }
      
      public function §final const dynamic§(param1:ClientObject) : §catch do§
      {
         var _loc2_:§'#o§ = §'#o§(param1.getParams(§null var implements§));
         return new §catch do§(_loc2_.§else var const§,_loc2_.§override try§,_loc2_.§function set do§);
      }
   }
}

