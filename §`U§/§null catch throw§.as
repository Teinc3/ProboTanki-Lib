package §`U§
{
   import flash.display.BitmapData;
   import utils.FlipBitmapDataUtils;
   
   public class §null catch throw§
   {
      private static const §class catch catch§:Class = §!#j§;
      
      public static const §extends for set§:BitmapData = new §class catch catch§().bitmapData;
      
      public static const §`,§:BitmapData = FlipBitmapDataUtils.flipH(§extends for set§);
      
      private static const §@!t§:Class = §while set override§;
      
      public static const §-v§:BitmapData = new §@!t§().bitmapData;
      
      public static const §["&§:BitmapData = FlipBitmapDataUtils.flipW(§-v§);
      
      private static const §implements var super§:Class = §super var switch§;
      
      public static const §static catch extends§:BitmapData = new §implements var super§().bitmapData;
      
      public static const §return set with§:BitmapData = FlipBitmapDataUtils.flipH(§static catch extends§);
      
      private static const §native package else§:Class = §final package switch§;
      
      public static const §<"Z§:BitmapData = new §native package else§().bitmapData;
      
      private static const §?!!§:Class = §const default§;
      
      public static const §if for each§:BitmapData = new §?!!§().bitmapData;
      
      public static const §each package use§:BitmapData = FlipBitmapDataUtils.flipH(§if for each§);
      
      private static const §catch for break§:Class = §switch var static§;
      
      public static const §?G§:BitmapData = new §catch for break§().bitmapData;
      
      public function §null catch throw§()
      {
         super();
      }
   }
}

