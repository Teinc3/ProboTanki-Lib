package controls
{
   import flash.display.Bitmap;
   import flash.display.BitmapData;
   
   public class §static set dynamic§ extends DefaultIconButton
   {
      private static const §false package continue§:Class = §get catch throw§;
      
      private static const § =§:BitmapData = (new §false package continue§() as Bitmap).bitmapData;
      
      public function §static set dynamic§()
      {
         super(§ =§);
      }
   }
}

