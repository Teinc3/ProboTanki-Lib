package §^#L§
{
   import alternativa.tanks.model.donationalert.DonationAlertModel;
   import alternativa.types.Long;
   import §if try§.§return package if§;
   
   public class §do for break§ extends §return package if§
   {
      private var §do for implements§:DonationAlertModel;
      
      public var modelId:int;
      
      public function §do for break§()
      {
         super();
         this.modelId = 69;
         this.§do for implements§ = DonationAlertModel(modelRegistry.getModel(Long.getLong(0,300050019)));
      }
      
      public function invoke(param1:Object) : void
      {
         switch(param1.getPacketId())
         {
            case § #x§.§7!o§:
               this.showDonationAlert(param1);
         }
      }
      
      private function showDonationAlert(param1:Object) : void
      {
         this.§do for implements§.showDonationAlert(Long.getLong(0,0),param1.donation,param1.packageBonusCrystals,param1.bonusCrystals,param1.image);
      }
   }
}

