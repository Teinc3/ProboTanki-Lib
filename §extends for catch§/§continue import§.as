package §extends for catch§
{
   public class §continue import§ extends §0#O§
   {
      private static const normalStateClass:Class = §return package try§;
      
      private static const overStateClass:Class = §[V§;
      
      public function §continue import§()
      {
         super();
         normalState = new normalStateClass().bitmapData;
         overState = new overStateClass().bitmapData;
      }
   }
}

