package §override package get§
{
   import alternativa.tanks.battle.§6!k§;
   import alternativa.tanks.battle.§else package false§;
   import alternativa.tanks.battle.§return const break§;
   
   public class §set const break§ extends §else package false§ implements §6!k§
   {
      private var controller:§return const break§;
      
      private var add:Boolean;
      
      public function §set const break§(param1:§return const break§, param2:Boolean)
      {
         super();
         this.controller = param1;
         this.add = param2;
      }
      
      public function execute() : void
      {
         if(this.add)
         {
            §'x§().§^c§(this.controller);
         }
         else
         {
            §'x§().§7`§(this.controller);
         }
      }
   }
}

