package §%3§
{
   import § !g§.§class for case§;
   
   public class MessageColor
   {
      public static const §finally package for§:uint = 65280;
      
      public static const §else catch else§:uint = 16776960;
      
      public static const BLUE:uint = 4691967;
      
      public static const RED:uint = 15741974;
      
      public static const DEFAULT:uint = 65280;
      
      public static const YELLOW:uint = 16776960;
      
      public static const §%#K§:uint = 16750081;
      
      public static const WHITE:uint = 14540253;
      
      public function MessageColor()
      {
         super();
      }
      
      public static function §true const each§(param1:§class for case§) : uint
      {
         switch(param1)
         {
            case §class for case§.BLUE:
               return MessageColor.BLUE;
            case §class for case§.RED:
               return MessageColor.RED;
            default:
               return MessageColor.DEFAULT;
         }
      }
      
      public static function §null function§(param1:§class for case§, param2:Boolean) : uint
      {
         if(param2)
         {
            return MessageColor.YELLOW;
         }
         switch(param1)
         {
            case §class for case§.BLUE:
               return MessageColor.BLUE;
            case §class for case§.RED:
               return MessageColor.RED;
            default:
               return MessageColor.DEFAULT;
         }
      }
      
      public static function §5!,§(param1:§class for case§, param2:Boolean, param3:Boolean) : uint
      {
         if(!param2)
         {
            return MessageColor.WHITE;
         }
         if(param3)
         {
            return 16777071;
         }
         switch(param1)
         {
            case §class for case§.BLUE:
               return 10013694;
            case §class for case§.RED:
               return 16685208;
            default:
               return MessageColor.DEFAULT;
         }
      }
      
      public static function §default set break§(param1:uint) : Boolean
      {
         return param1 == MessageColor.BLUE || param1 == MessageColor.RED;
      }
   }
}

