package §4z§
{
   public class §'"3§
   {
      private static const §break for override§:Number = 8 * 180 / Math.PI;
      
      private static const §with for break§:Number = 90;
      
      public function §'"3§()
      {
         super();
      }
      
      private static function §5#4§(param1:Number) : int
      {
         var _loc2_:Number = NaN;
         _loc2_ = §break for override§;
         return Math.ceil(param1 * _loc2_);
      }
      
      public static function §7A§(param1:Number) : Number
      {
         return Math.min(§break for override§,1 / (2 * Math.atan(§with for break§ / (2 * param1))));
      }
   }
}

